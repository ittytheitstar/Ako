# IMPORTANT: Replace placeholder values before deploying.
# Use kubectl create secret or a secrets manager in production.
apiVersion: v1
kind: Secret
metadata:
  name: ako-secrets
  namespace: ako
type: Opaque
stringData:
  DATABASE_URL: "postgresql://ako:CHANGE_ME@postgres:5432/ako"
  JWT_SECRET: "CHANGE_TO_32_CHAR_RANDOM_SECRET_!!"
  REDIS_URL: "redis://redis:6379"
